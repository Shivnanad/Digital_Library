{
  "buildCommand": "cd frontend/react-app && npm install && npm run build",
  "outputDirectory": "frontend/react-app/dist",
  "installCommand": "cd frontend/react-app && npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
