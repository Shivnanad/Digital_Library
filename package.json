{
  "name": "digital-library",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Digital Library with AI Voice Search, Book Catalog, and Admin Panel",
  "scripts": {
    "dev:frontend": "npm --prefix frontend/react-app run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend/react-app run build",
    "start": "npm --prefix backend start"
  }
}
